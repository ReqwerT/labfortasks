### Locale ve klavye ayarları
d-i debian-installer/language string en
d-i debian-installer/country string US
d-i debian-installer/locale string en_US.UTF-8

d-i console-setup/ask_detect boolean false
d-i keyboard-configuration/xkb-keymap select us

### Ağ ayarları
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string debian-vm
d-i netcfg/get_domain string local

### Mirror ayarları
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

### Kullanıcı ve şifreler
d-i passwd/root-password password rootpass
d-i passwd/root-password-again password rootpass

d-i passwd/user-fullname string User
d-i passwd/username string user
d-i passwd/user-password password userpass
d-i passwd/user-password-again password userpass

d-i passwd/user-fullname string Sefo
d-i passwd/username string sefo
d-i passwd/user-password password 1647
d-i passwd/user-password-again password 1647

### Saat ve zaman dilimi
d-i clock-setup/utc boolean true
d-i time/zone string Europe/Istanbul

### Disk bölümlendirme ve formatlama
d-i partman-auto/method string regular
d-i partman-auto/disk string /dev/vda
d-i partman-auto/choose_recipe select atomic
d-i partman-auto/erase-disk boolean true
d-i partman/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

### Masaüstü ve paketler
tasksel tasksel/first multiselect standard
d-i pkgsel/include string openssh-server kde-full
d-i pkgsel/upgrade select none

### GRUB kurulumu
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i grub-installer/bootdev string /dev/vda

### Kurulum bitiş
d-i finish-install/reboot_in_progress note

### Tam otomasyon
d-i preseed/priority string critical
d-i preseed/interactive boolean false

